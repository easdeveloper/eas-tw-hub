(() => {
    'use strict';
    EAS.MarketOffersExecution = EAS.MarketOffersExecution || {};
    const STORAGE_KEY = 'eas_tw_market_offers_execution';
    const HISTORY_KEY = 'eas_tw_market_offers_history';
    const CHANNEL_NAME = 'eas_tw_market_offers_channel';
    const PANEL_ID = 'eas-market-offer-execution-panel';
    const EXECUTION_VERSION = 3;
    const DEBUG_LOG_KEY = 'eas_tw_market_offers_debug_log';
    const EXECUTION_WINDOW_KEY = 'eas_tw_market_execution_window';
    const RETURN_MESSAGE_SOURCE = 'eas-tw-hub';
    const DEFAULT_MAX_TRAVEL_HOURS = 15;
    const MARKET_SELECTORS = { repetitions: 'input[name="multi"]', maxTravelTime: 'input[name="max_time"]', createButton: 'input[type="submit"][value="Criar"]' };
    const TERMINAL = new Set(['created', 'skipped', 'cancelled', 'canceled']);
    const amount = (value) => Math.max(0, Math.floor(Number(value) || 0));
    const isOwnOfferPage = (targetWindow = window) => { try { const params = new URL(targetWindow.location.href).searchParams; return params.get('screen') === 'market' && params.get('mode') === 'own_offer'; } catch { return false; } };
    const getDebugLogs = () => { try { return JSON.parse(localStorage.getItem(DEBUG_LOG_KEY) || '[]'); } catch { return []; } };
    const clearDebugLogs = () => { try { localStorage.removeItem(DEBUG_LOG_KEY); } catch {} };
    const logMarketOfferExecution = (event, data = {}) => { const entry = { timestamp: Date.now(), pageUrl: location.href, villageId: window.game_data?.village?.id ?? null, event, data }; try { const logs = getDebugLogs(); logs.push(entry); localStorage.setItem(DEBUG_LOG_KEY, JSON.stringify(logs.slice(-200))); } catch {} console.debug('[EAS Market Offers]', event, data); return entry; };
    const recordItemEvent = (context, item, event, data = {}, targetWindow = window) => { const url = new URL(targetWindow.location.href); const entry = logMarketOfferExecution(event, { executionId: context?.executionId, itemId: item?.id, villageId: item?.villageId, ...data }); if (item) { item.diagnostic ||= {}; Object.assign(item.diagnostic, { executionId: context?.executionId, itemId: item.id, villageId: item.villageId, villageName: item.villageName, offerResource: item.offerResource, receiveResource: item.requestResource, amountPerOffer: item.amountPerOffer, repetitions: item.repeatCount, totalOffer: item.totalOfferAmount, expectedMerchants: item.merchantsRequired, currentUrl: url.href, pageMode: url.searchParams.get('mode'), persistedStatus: item.status, currentRuntimeStatus: getRuntime(targetWindow)?.preparing ? 'preparing' : item.status, lastEvent: event, lastEventAt: entry.timestamp, ...data }); item.diagnosticEvents = [...(item.diagnosticEvents || []), entry].slice(-20); if (context) save(context); } return entry; };
    const summarizeOfferExecution = (execution) => { const queue = execution?.queue || []; const activeIndex = queue.findIndex((item, index) => index >= Number(execution?.currentIndex || 0) && !TERMINAL.has(item.status)); const currentIndex = activeIndex < 0 ? queue.length : activeIndex; const currentItem = queue[currentIndex] || null; const nextItem = queue.slice(currentIndex + 1).find((item) => !TERMINAL.has(item.status)) || null; const statuses = { pending: 0, prepared: 0, submitting: 0, created: 0, skipped: 0, error: 0 }; queue.forEach((item) => { if (Object.hasOwn(statuses, item.status)) statuses[item.status] += 1; }); return { executionVersion: execution?.version ?? null, currentIndex, queueLength: queue.length, statuses, currentItemId: currentItem?.id || null, currentItemVillageId: currentItem?.villageId || null, nextItemId: nextItem?.id || null, nextItemVillageId: nextItem?.villageId || null }; };
    const read = () => { try { const context = JSON.parse(localStorage.getItem(STORAGE_KEY) || 'null'); if (context && (!Number.isFinite(Number(context.version)) || Number(context.version) < EXECUTION_VERSION)) { logMarketOfferExecution('execution-cleared', { reason: 'obsolete-version', summary: summarizeOfferExecution(context) }); localStorage.removeItem(STORAGE_KEY); return null; } return context; } catch (error) { logMarketOfferExecution('unexpected-error', { stage: 'read-execution', message: error.message }); return null; } };
    const save = (context) => { try { localStorage.setItem(STORAGE_KEY, JSON.stringify(context)); return true; } catch { return false; } };
    const remove = (reason = 'explicit-remove') => { const execution = read(); logMarketOfferExecution('execution-cleared', { reason, summary: summarizeOfferExecution(execution) }); try { localStorage.removeItem(STORAGE_KEY); } catch {} };
    const emit = (message) => { try { const channel = new BroadcastChannel(CHANNEL_NAME); channel.postMessage(message); channel.close(); } catch {} };
    const isExecutionFinished = (execution) => Boolean(execution && (execution.finishedAt || execution.endedAt || (execution.queue || []).every((item) => TERMINAL.has(item.status))));
    const getMenuRuntime = (targetWindow = window) => targetWindow.EASMarketOfferMenuRuntime ||= { listenerInitialized: false, executionWindows: new Map(), handledExecutions: new Set(), channel: null };
    const completionSummary = (execution) => ({ created: (execution?.queue || []).filter((item) => item.status === 'created').length, errors: (execution?.queue || []).filter((item) => ['error', 'verification-required'].includes(item.status)).length, skipped: (execution?.queue || []).filter((item) => ['skipped', 'cancelled', 'canceled'].includes(item.status)).length });
    const refreshExistingHubPanel = (targetWindow = window, execution = read()) => {
        const doc = targetWindow.document; const panel = doc?.querySelector('#eas-tw-hub-root, #eas-tw-hub-panel, #eas-market-offers');
        if (!panel) return false;
        const card = panel.querySelector('[data-eas-module="market-smart-offers"]');
        if (card) { const oldBadge = card.querySelector('.hub-tool-card-badge'); const indicator = targetWindow.EAS?.UI?.getDashboardIndicator?.('market-smart-offers'); if (oldBadge) oldBadge.textContent = indicator || ''; else if (indicator) { const badge = doc.createElement('span'); badge.className = 'hub-tool-card-badge'; badge.textContent = indicator; card.querySelector('.hub-tool-card-content')?.appendChild(badge); } }
        let status = panel.querySelector('[data-market-return-status]');
        if (!status) { status = doc.createElement('div'); status.dataset.marketReturnStatus = 'true'; status.className = 'eas-status eas-status--success market-return-status'; (panel.querySelector('.hub-dashboard, .market-offers, .eas-window__body') || panel).prepend(status); }
        status.textContent = 'Execução de ofertas concluída.';
        panel.dispatchEvent(new targetWindow.CustomEvent('eas-market-offers-execution-finished', { detail: { executionId: execution?.executionId, summary: completionSummary(execution) } }));
        return true;
    };
    const handleReturnMessage = (data, targetWindow = window, sourceWindow = null, viaBroadcast = false) => {
        if (!data || (!viaBroadcast && data.source !== RETURN_MESSAGE_SOURCE) || !['market-offers-execution-finished', 'execution-finished'].includes(data.type) || !data.executionId) return false;
        const execution = read(); if (!execution || execution.executionId !== data.executionId) return false;
        const runtime = getMenuRuntime(targetWindow); const expected = runtime.executionWindows.get(data.executionId);
        if (!viaBroadcast && (!sourceWindow || !expected || sourceWindow !== expected)) return false;
        if (runtime.handledExecutions.has(data.executionId)) return true;
        runtime.handledExecutions.add(data.executionId); refreshExistingHubPanel(targetWindow, execution); return true;
    };
    const registerMarketOfferMenuListener = (targetWindow = window) => {
        const runtime = getMenuRuntime(targetWindow); if (runtime.listenerInitialized) return runtime; runtime.listenerInitialized = true;
        targetWindow.addEventListener?.('message', (event) => { if (event.origin !== targetWindow.location.origin) return; handleReturnMessage(event.data, targetWindow, event.source, false); });
        try { runtime.channel = new targetWindow.BroadcastChannel(CHANNEL_NAME); runtime.channel.onmessage = (event) => handleReturnMessage(event.data, targetWindow, null, true); } catch {}
        return runtime;
    };
    const renderReturnFallback = (targetWindow, execution, reason) => {
        const doc = targetWindow.document; const panel = doc?.getElementById(PANEL_ID); if (!panel) return false;
        let fallback = panel.querySelector('[data-market-return-fallback]'); if (!fallback) { fallback = doc.createElement('div'); fallback.dataset.marketReturnFallback = 'true'; fallback.className = 'eas-status eas-status--warning market-return-fallback'; panel.appendChild(fallback); }
        fallback.innerHTML = `<strong>Não foi possível fechar esta aba automaticamente.</strong><span>${EAS.Utils.escapeHtml(reason)}</span><div class="fake-execution-actions"><button data-open-hub>Abrir Hub aqui</button><button data-retry-close>Tentar fechar novamente</button><button data-copy-return>Copiar diagnóstico</button></div>`;
        fallback.querySelector('[data-open-hub]').onclick = () => targetWindow.EAS?.UI?.openMainWindow?.();
        fallback.querySelector('[data-retry-close]').onclick = () => { if (targetWindow.sessionStorage?.getItem(EXECUTION_WINDOW_KEY) === execution.executionId) targetWindow.close(); };
        fallback.querySelector('[data-copy-return]').onclick = async () => { const diagnostic = JSON.stringify({ reason, executionId: execution.executionId, openerAvailable: Boolean(targetWindow.opener && !targetWindow.opener.closed), marker: targetWindow.sessionStorage?.getItem(EXECUTION_WINDOW_KEY), summary: completionSummary(execution) }, null, 2); try { await targetWindow.navigator.clipboard.writeText(diagnostic); } catch { targetWindow.prompt?.('Copie o diagnóstico:', diagnostic); } };
        return true;
    };
    const returnToMarketMenu = ({ targetWindow = window, context = read(), render = () => {} } = {}) => {
        if (!context) return { state: 'missing' }; const runtime = getRuntime(targetWindow);
        if (runtime.returning) return { state: 'returning' };
        if (!isExecutionFinished(context)) { render('Ainda existem ofertas pendentes. A execução continuará aberta nesta aba.', 'warning'); refreshExistingHubPanel(targetWindow, context); return { state: 'pending' }; }
        runtime.returning = true; context.finishedAt ||= Date.now(); context.returnTarget ||= 'market-offers-menu'; save(context);
        const message = { source: RETURN_MESSAGE_SOURCE, type: 'market-offers-execution-finished', executionId: context.executionId, summary: completionSummary(context) };
        if (runtime.returnNotifiedExecutionId !== context.executionId) { runtime.returnNotifiedExecutionId = context.executionId; emit({ type: 'execution-finished', executionId: context.executionId, summary: message.summary }); }
        const opener = targetWindow.opener; const openerAvailable = Boolean(opener && !opener.closed); if (openerAvailable) { try { opener.postMessage(message, targetWindow.location.origin); opener.focus?.(); } catch {} }
        const markerMatches = targetWindow.sessionStorage?.getItem(EXECUTION_WINDOW_KEY) === context.executionId;
        if (!openerAvailable || !markerMatches) { runtime.returning = false; renderReturnFallback(targetWindow, context, !openerAvailable ? 'A aba principal não está disponível.' : 'Esta aba não corresponde à janela registrada para a execução.'); return { state: 'fallback' }; }
        targetWindow.setTimeout(() => { targetWindow.close(); targetWindow.setTimeout(() => { if (!targetWindow.closed) { runtime.returning = false; renderReturnFallback(targetWindow, context, 'O navegador bloqueou o fechamento automático.'); } }, 250); }, 100);
        return { state: 'closing' };
    };
    const getRuntime = (targetWindow = window) => targetWindow.EASMarketOfferRuntime ||= { initialized: false, preparing: false, preparedItemId: null, paused: false, observer: null, timers: new Set(), prepareAttempts: new Map() };
    const cleanupMarketOfferRuntime = (targetWindow = window) => { const runtime = getRuntime(targetWindow); logMarketOfferExecution('runtime-cleanup', { initialized: runtime.initialized, preparedItemId: runtime.preparedItemId, timers: runtime.timers.size }); runtime.observer?.disconnect?.(); runtime.observer = null; runtime.timers.forEach((timer) => { targetWindow.clearTimeout(timer); targetWindow.clearInterval(timer); }); runtime.timers.clear(); runtime.preparing = false; return runtime; };
    const exportDiagnostic = (targetWindow = window) => { const execution = read(); const runtime = getRuntime(targetWindow); const currentItem = execution ? current(execution) : null; const url = new URL(targetWindow.location?.href || location.href); const tableSnapshot = currentItem ? snapshotOffers(targetWindow.document, currentItem) : snapshotOffers(targetWindow.document, {}); return { exportedAt: Date.now(), execution, currentItem, currentPage: { url: url.href, screen: url.searchParams.get('screen'), mode: url.searchParams.get('mode'), villageId: getVillageId(targetWindow), formDetected: tableSnapshot.formDetected, ownOffersDetected: tableSnapshot.ownOffersDetected }, expectedOffer: currentItem ? { offerResource: currentItem.offerResource, offerAmount: currentItem.offerAmount, receiveResource: currentItem.requestResource, receiveAmount: currentItem.requestAmount, repetitions: currentItem.repeatCount, maxTravelHours: currentItem.maxTravelHours } : null, tableSnapshot, runtimeState: { initialized: runtime.initialized, preparing: runtime.preparing, preparedItemId: runtime.preparedItemId, paused: runtime.paused, timerCount: runtime.timers.size, observerActive: Boolean(runtime.observer) }, persistedState: currentItem?.diagnostic || null, timestamps: { executionCreatedAt: execution?.createdAt || null, clickTimestamp: currentItem?.clickTimestamp || null, navigationTimestamp: currentItem?.diagnostic?.navigationTimestamp || null, verificationRequiredAt: currentItem?.verificationRequiredAt || null }, lastEvents: getDebugLogs().slice(-20) }; };
    const registerPrepareAttempt = (runtime, itemId, now = Date.now()) => { const attempts = (runtime.prepareAttempts.get(itemId) || []).filter((timestamp) => now - timestamp < 10000); attempts.push(now); runtime.prepareAttempts.set(itemId, attempts); if (attempts.length > 3) runtime.paused = true; return { allowed: !runtime.paused, count: attempts.length }; };
    const addHistory = (item, result, error = null) => { try { const history = JSON.parse(localStorage.getItem(HISTORY_KEY) || '[]'); history.unshift({ executedAt: Date.now(), villageId: item.villageId, offerResource: item.offerResource, offerAmount: item.offerAmount, requestResource: item.requestResource, requestAmount: item.requestAmount, repeatCount: item.repeatCount, totalOfferAmount: item.totalOfferAmount, totalRequestAmount: item.totalRequestAmount, result, error }); localStorage.setItem(HISTORY_KEY, JSON.stringify(history.slice(0, 200))); } catch {} };
    const normalizeItem = (item) => {
        const repeatCount = Math.max(1, amount(item.repeatCount || 1)); const offerAmount = amount(item.amountPerOffer || item.offerAmount); const requestAmount = amount(item.requestAmountPerOffer || item.requestAmount);
        const totalOfferAmount = amount(item.totalOfferAmount || offerAmount * repeatCount); const totalRequestAmount = amount(item.totalRequestAmount || requestAmount * repeatCount);
        return { ...item, id: item.id || `queue-${Date.now()}-${Math.random().toString(36).slice(2)}`, offerAmount, amountPerOffer: offerAmount, requestAmount, requestAmountPerOffer: requestAmount, repeatCount, totalOfferAmount, totalRequestAmount, merchantsRequired: amount(item.merchantsRequired || EAS.MarketEngine.calculateMerchantsRequired({ [item.offerResource]: totalOfferAmount })), maxTravelHours: Math.max(1, amount(item.maxTravelHours || DEFAULT_MAX_TRAVEL_HOURS)), status: item.status || 'pending', error: item.error || null };
    };
    const nextPendingIndex = (context, start = 0) => {
        const index = (context.queue || []).findIndex((item, itemIndex) => itemIndex >= start && !TERMINAL.has(item.status));
        return index < 0 ? context.queue.length : index;
    };
    const current = (context) => context?.queue?.[nextPendingIndex(context, context.currentIndex || 0)] || null;
    const syncIndex = (context) => { context.currentIndex = nextPendingIndex(context, context.currentIndex || 0); return context.currentIndex; };
    const getCurrentPendingOfferItem = (execution) => { if (!execution) return null; syncIndex(execution); return current(execution); };
    const getVillageId = (targetWindow) => String(new URL(targetWindow.location.href).searchParams.get('village') || targetWindow.game_data?.village?.id || '');
    const marketUrl = (villageId) => { const url = new URL('/game.php', location.origin); url.searchParams.set('village', villageId); url.searchParams.set('screen', 'market'); url.searchParams.set('mode', 'own_offer'); return url; };
    const injectLoader = (targetWindow) => {
        logMarketOfferExecution('loader-start', { targetUrl: targetWindow?.location?.href || null }); try { const doc = targetWindow.document; if (targetWindow.EAS?.MarketOffersExecution?.initialize) { targetWindow.EAS.MarketOffersExecution.initialize(); return true; } if (doc.querySelector('script[data-eas-market-auto-loader]')) return true; const script = doc.createElement('script'); script.src = `https://easdeveloper.github.io/eas-tw-hub/index.js?v=${Date.now()}`; script.dataset.easMarketAutoLoader = 'true'; (doc.head || doc.documentElement).appendChild(script); return true; } catch (error) { logMarketOfferExecution('unexpected-error', { stage: 'inject-loader', message: error.message, stack: error.stack }); return false; }
    };
    const watchExecutionWindow = (targetWindow, ownerWindow = window) => {
        if (!targetWindow || targetWindow.closed) return null; if (targetWindow.__easMarketWatchTimer) return targetWindow.__easMarketWatchTimer;
        const timer = ownerWindow.setInterval(() => { if (targetWindow.closed || !read() || read()?.endedAt) { ownerWindow.clearInterval(timer); return; } try { if (targetWindow.document?.readyState !== 'loading' && !targetWindow.EAS?.MarketOffersExecution?.initialize) injectLoader(targetWindow); else if (!targetWindow.document?.getElementById(PANEL_ID)) targetWindow.EAS?.MarketOffersExecution?.initialize?.(); } catch {} }, 300);
        try { targetWindow.__easMarketWatchTimer = timer; } catch {} return timer;
    };
    const openCurrent = (context) => {
        syncIndex(context); const item = current(context); logMarketOfferExecution(item ? 'current-item-found' : 'current-item-missing', summarizeOfferExecution(context)); if (!item) return false;
        item.status = item.status === 'error' || item.status === 'verification-required' ? item.status : 'opening'; context.popupName = `eas-market-offer-${context.executionId}`; save(context);
        const targetUrl = String(marketUrl(item.villageId)); logMarketOfferExecution('navigation-target', { targetUrl, itemId: item.id, villageId: item.villageId });
        if (window.name === context.popupName) { try { window.sessionStorage.setItem(EXECUTION_WINDOW_KEY, context.executionId); } catch {} logMarketOfferExecution('navigation-start', { currentVillageId: window.game_data?.village?.id, nextVillageId: item.villageId, nextItemId: item.id, targetUrl }); window.location.assign(targetUrl); return true; }
        const popup = window.open(targetUrl, context.popupName); if (!popup) return false;
        try { popup.sessionStorage.setItem(EXECUTION_WINDOW_KEY, context.executionId); } catch {}
        getMenuRuntime(window).executionWindows.set(context.executionId, popup);
        try { popup.addEventListener('load', () => injectLoader(popup)); } catch {}
        watchExecutionWindow(popup, window);
        return true;
    };
    const setValue = (input, value, targetWindow) => { const setter = Object.getOwnPropertyDescriptor(targetWindow.HTMLInputElement.prototype, 'value')?.set; setter ? setter.call(input, String(value)) : input.value = String(value); input.dispatchEvent(new targetWindow.Event('input', { bubbles: true })); input.dispatchEvent(new targetWindow.Event('change', { bubbles: true })); };
    const findField = (doc, names) => names.map((name) => doc.querySelector(`[name="${name}"], #${name}, [data-field="${name}"]`)).find(Boolean) || null;
    const findRepeatField = (doc) => doc.querySelector(MARKET_SELECTORS.repetitions);
    const fillRepeatCount = (doc, item, targetWindow = window) => {
        const multiInput = findRepeatField(doc); if (!multiInput) return { valid: false, message: 'Campo Quantas vezes oferecer não encontrado.' };
        const repeatCount = Number(item.repeatCount); if (!Number.isInteger(repeatCount) || repeatCount <= 0) return { valid: false, message: 'repeatCount inválido.' };
        const debug = localStorage.getItem('eas_tw_market_offers_debug') === 'true';
        if (debug) console.debug('[EAS Market Offer]', { repeatCountFromQueue: item.repeatCount, merchantsAvailable: item.merchantsAvailable, fieldBefore: multiInput.value });
        const apply = () => { multiInput.focus(); const setter = Object.getOwnPropertyDescriptor(targetWindow.HTMLInputElement.prototype, 'value')?.set; setter ? setter.call(multiInput, String(repeatCount)) : multiInput.value = String(repeatCount); multiInput.dispatchEvent(new targetWindow.Event('input', { bubbles: true })); multiInput.dispatchEvent(new targetWindow.Event('change', { bubbles: true })); multiInput.blur(); };
        apply(); if (Number(multiInput.value) !== repeatCount) apply();
        if (debug) console.debug('[EAS Market Offer]', { fieldAfter: multiInput.value });
        return { valid: Number(multiInput.value) === repeatCount, input: multiInput, repeatCount, apply, message: Number(multiInput.value) === repeatCount ? null : 'Não foi possível preencher Quantas vezes oferecer.' };
    };
    const setOfferRepeatCount = async (doc, item, targetWindow = window) => {
        const input = findRepeatField(doc); const expected = Number(item.repeatCount); if (!input) return { valid: false, message: 'Campo Quantas vezes oferecer não encontrado.' };
        if (!Number.isInteger(expected) || expected <= 0) return { valid: false, message: 'repeatCount inválido.' };
        const sleep = (milliseconds) => new Promise((resolve) => targetWindow.setTimeout(resolve, milliseconds)); const startedAt = Date.now(); const initialValue = input.value; const timeline = [{ elapsedMs: 0, phase: 'start', readyState: doc.readyState, value: input.value }];
        if (doc.readyState !== 'complete') await new Promise((resolve) => targetWindow.addEventListener('load', resolve, { once: true }));
        const navigation = targetWindow.performance?.getEntriesByType?.('navigation')?.[0]; timeline.push({ elapsedMs: Date.now() - startedAt, phase: 'window.load', readyState: doc.readyState, value: input.value, domContentLoadedMs: Math.round(navigation?.domContentLoadedEventEnd || 0), loadMs: Math.round(navigation?.loadEventEnd || 0) });
        let observedValue = input.value;
        for (let sample = 1; sample <= 10; sample += 1) { await sleep(50); if (input.value !== observedValue) { observedValue = input.value; timeline.push({ elapsedMs: Date.now() - startedAt, phase: 'tribal-initialization', value: input.value }); } }
        let result = null; let stableAttempt = null; let overwriteDetectedAtMs = null;
        for (let attempt = 1; attempt <= 5; attempt += 1) {
            const before = input.value; result = fillRepeatCount(doc, item, targetWindow); timeline.push({ elapsedMs: Date.now() - startedAt, phase: `fill-${attempt}`, before, after: input.value });
            await sleep(200); const actual = Number(input.value); timeline.push({ elapsedMs: Date.now() - startedAt, phase: `verify-${attempt}`, value: input.value });
            if (actual === expected) { stableAttempt = attempt; break; }
            overwriteDetectedAtMs = Date.now() - startedAt; timeline.push({ elapsedMs: overwriteDetectedAtMs, phase: `overwrite-after-fill-${attempt}`, expected, actual });
        }
        const actual = Number(input.value); const valid = actual === expected; const stabilizedAtMs = valid ? Date.now() - startedAt : null; const diagnostics = { expected, initialValue, finalValue: input.value, stableAttempt, overwriteDetectedAtMs, stabilizedAtMs, timeline };
        console.debug('[EAS Offer Multi Timing]', diagnostics);
        return { ...result, valid, initialValue, finalValue: input.value, stableAttempt, stabilizedAtMs, diagnostics, message: valid ? null : `Falha ao preparar repetições após 5 tentativas. Esperado: ${expected}. Atual: ${actual}.` };
    };
    const findSubmit = (doc, form) => form?.querySelector(`${MARKET_SELECTORS.createButton}, [name="create_offer"], button[type="submit"], input[type="submit"], [data-action="create-offer"]`) || doc.querySelector(`${MARKET_SELECTORS.createButton}, [name="create_offer"], [data-action="create-offer"]`);
    const waitForElement = (doc, selector, timeoutMs = 5000, targetWindow = window) => new Promise((resolve) => { const existing = doc.querySelector(selector); if (existing) { resolve(existing); return; } let settled = false; let observer = null; const finish = (value) => { if (settled) return; settled = true; observer?.disconnect(); targetWindow.clearTimeout(timer); resolve(value); }; const Observer = targetWindow.MutationObserver || window.MutationObserver; if (Observer) { observer = new Observer(() => { const element = doc.querySelector(selector); if (element) finish(element); }); observer.observe(doc.documentElement || doc, { childList: true, subtree: true }); } const timer = targetWindow.setTimeout(() => finish(doc.querySelector(selector)), Math.max(0, Number(timeoutMs) || 0)); });
    const waitForMaxTravelTimeInput = (doc, timeoutMs = 5000, targetWindow = window) => waitForElement(doc, MARKET_SELECTORS.maxTravelTime, timeoutMs, targetWindow);
    const chooseResource = (doc, kind, resource, targetWindow = window) => {
        const names = kind === 'offer' ? ['res_sell', 'sell_resource', 'offer_resource'] : ['res_buy', 'buy_resource', 'request_resource'];
        const select = names.map((name) => doc.querySelector(`select[name="${name}"], select#${name}`)).find(Boolean);
        if (select) { select.value = resource; select.dispatchEvent(new targetWindow.Event('change', { bubbles: true })); return select.value === resource; }
        const input = names.flatMap((name) => [...doc.querySelectorAll(`input[name="${name}"]`)]).find((entry) => entry.value === resource) || doc.querySelector(`#${names[0]}_${resource}, [data-${kind}-resource="${resource}"]`);
        if (input) { input.click(); return true; } return false;
    };
    const errorMessage = (doc) => {
        if (doc.querySelector('form#login, input[name="password"]')) return 'Sessão expirada';
        const text = [...doc.querySelectorAll('.error, .error_box, .error-message, #error, .warn, .warning')].map((node) => node.textContent?.trim()).find(Boolean);
        if (!text) return null;
        const known = [/comerciante/i, /recurso/i, /proporção|proporcao/i, /limite.*oferta/i, /quantidade/i, /mercado.*indisponível|mercado.*indisponivel/i, /sessão|sessao/i];
        return known.some((pattern) => pattern.test(text)) ? text : `Erro do Mercado: ${text}`;
    };
    const snapshotOffers = (doc, expectedOffer = {}) => {
        const parsed = EAS.MarketEngine.parseMarketVillageDocument(doc, { id: expectedOffer.villageId || '' }); const rows = parsed.activeOfferList;
        const matchingRows = rows.filter((offer) => offer.offerResource === expectedOffer.offerResource && offer.requestResource === expectedOffer.requestResource && amount(offer.offerAmount) === amount(expectedOffer.offerAmount) && amount(offer.requestAmount) === amount(expectedOffer.requestAmount));
        const durationField = doc.querySelector(`${MARKET_SELECTORS.maxTravelTime}, select[name="duration"], select[name="time"], input[name="duration"], input[name="time"]`);
        const offers = rows.map((offer) => ({ offerId: offer.offerId || null, offerResource: offer.offerResource, offerAmount: amount(offer.offerAmount), receiveResource: offer.requestResource, receiveAmount: amount(offer.requestAmount), quantity: Math.max(1, amount(offer.repeatCount)), maxTravelTime: offer.maxTravelTime ?? offer.duration ?? null }));
        return { offerCount: rows.length, matchingOffers: matchingRows.length, matchingQuantity: matchingRows.reduce((sum, offer) => sum + Math.max(1, amount(offer.repeatCount)), 0), rows, offers, ownOffersDetected: Boolean(doc.querySelector('#own_offers_table, [data-own-offers], .own_offers')), formDetected: Boolean(doc.querySelector('form[action*="screen=market"], input[name="sell"], input[name="offer_amount"]')), merchantsAvailable: parsed.merchants?.available ?? null, merchantsTotal: parsed.merchants?.total ?? null, maxTravelTime: durationField?.value ?? null, successMessages: [...doc.querySelectorAll('.success, .success_box, .success-message')].map((node) => node.textContent?.trim()).filter(Boolean) };
    };
    const detectOfferCreationSuccess = ({ document: doc, expectedOffer, previousSnapshot }) => {
        const currentSnapshot = snapshotOffers(doc, expectedOffer); const previous = previousSnapshot || { offerCount: 0, matchingOffers: 0, matchingQuantity: 0, successMessages: [] };
        const newCompatibleOffer = currentSnapshot.matchingOffers > previous.matchingOffers;
        const groupedQuantityIncreased = currentSnapshot.matchingQuantity > previous.matchingQuantity;
        const explicitSuccess = currentSnapshot.successMessages.some((message) => !previous.successMessages?.includes(message));
        if (!newCompatibleOffer && !groupedQuantityIncreased && !explicitSuccess) return { success: false, snapshot: currentSnapshot };
        const compatible = currentSnapshot.rows.find((offer) => offer.offerResource === expectedOffer.offerResource && offer.requestResource === expectedOffer.requestResource && amount(offer.offerAmount) === amount(expectedOffer.offerAmount) && amount(offer.requestAmount) === amount(expectedOffer.requestAmount));
        return { success: true, offerId: compatible?.offerId || null, detectedAmount: compatible?.offerAmount || expectedOffer.offerAmount, detectedQuantity: Math.max(1, currentSnapshot.matchingQuantity - previous.matchingQuantity || expectedOffer.repeatCount || 1), evidence: newCompatibleOffer ? 'new-compatible-offer' : groupedQuantityIncreased ? 'grouped-quantity-increased' : 'explicit-success', snapshot: currentSnapshot };
    };
    const detectCreatedSmartOffer = (item, doc = document, previousSnapshot = item?.previousSnapshot) => detectOfferCreationSuccess({ document: doc, expectedOffer: item, previousSnapshot });
    const setMaxTravelHours = (doc, hours, targetWindow = window, detectedField = null) => { const expected = Math.max(1, amount(hours || DEFAULT_MAX_TRAVEL_HOURS)); const primaryField = detectedField || doc.querySelector(MARKET_SELECTORS.maxTravelTime); const field = primaryField || doc.querySelector('select[name="duration"], select[name="time"], input[name="duration"], input[name="time"]'); if (!field) return { valid: false, expected, value: null, selector: null, message: 'Campo Duração máxima da viagem não encontrado.' }; let displayedHours = expected; if (field.tagName === 'SELECT') { const option = [...field.options].find((entry) => Number(entry.value) === expected || Number(entry.value) === expected * 3600 || new RegExp(`(^|\\D)${expected}\\s*h`, 'i').test(entry.textContent)); if (option) { field.value = option.value; displayedHours = expected; } else { field.value = String(expected); displayedHours = Number(field.value); } field.dispatchEvent(new targetWindow.Event('input', { bubbles: true })); field.dispatchEvent(new targetWindow.Event('change', { bubbles: true })); } else { setValue(field, String(expected), targetWindow); displayedHours = Number(field.value); } const valid = displayedHours === expected; return { valid, expected, value: field.value, selector: primaryField ? MARKET_SELECTORS.maxTravelTime : 'legacy-fallback', message: valid ? null : `Não foi possível configurar a duração máxima. Esperado: ${expected}. Atual: ${field.value}.` }; };
    const classifyOfferResult = (doc, item, initialOfferCount = 0) => {
        const error = errorMessage(doc); if (error) return { status: 'error', error };
        const result = detectOfferCreationSuccess({ document: doc, expectedOffer: item, previousSnapshot: { offerCount: initialOfferCount, matchingOffers: 0, matchingQuantity: 0, successMessages: [] } });
        return result.success ? { status: 'created', offerId: result.offerId, evidence: result.evidence } : null;
    };
    const validateQueueItem = (item) => {
        const cache = EAS.MarketEngine.getCache(); const village = EAS.MarketEngine.cacheVillages(cache).find((entry) => String(entry.villageId) === String(item.villageId)); const reasons = [];
        if (!village?.available) reasons.push('Dados incompletos');
        if (Date.now() - Number(village?.updatedAt || 0) > 15 * 60 * 1000) reasons.push('Dados alterados ou desatualizados');
        if (!(item.offerAmount > 0) || !(item.requestAmount > 0) || !(item.repeatCount > 0) || item.offerResource === item.requestResource) reasons.push('Oferta inválida');
        if (village && EAS.MarketEngine.getAvailableResources(village)[item.offerResource] < item.totalOfferAmount) reasons.push('Recursos insuficientes');
        if (!(item.totalRequestAmount > 0)) reasons.push('Total solicitado inválido');
        if (village && item.repeatCount > village.merchants.available) reasons.push('Repetições acima dos comerciantes livres');
        if (village && village.merchants.available < item.merchantsRequired) reasons.push('Comerciantes insuficientes');
        return { valid: reasons.length === 0, reasons, village };
    };
    const prepareItem = async (context, targetWindow) => {
        syncIndex(context); const item = current(context); if (!item || getVillageId(targetWindow) !== String(item.villageId)) return { valid: false, message: 'Abra o Mercado da aldeia correta.' };
        const validation = validateQueueItem(item); if (!validation.valid) { item.status = 'error'; item.error = validation.reasons.join(', '); save(context); return { valid: false, message: item.error }; }
        const doc = targetWindow.document; const offerAmount = findField(doc, ['sell', 'offer_amount', 'amount_sell']); const requestAmount = findField(doc, ['buy', 'request_amount', 'amount_buy']); const repeatCount = findRepeatField(doc);
        if (!offerAmount || !requestAmount || !chooseResource(doc, 'offer', item.offerResource, targetWindow) || !chooseResource(doc, 'request', item.requestResource, targetWindow)) return { valid: false, message: 'Campos de criação de oferta não encontrados.' };
        const form = offerAmount.closest('form') || requestAmount.closest('form'); const submit = findSubmit(doc, form); if (submit) submit.disabled = true;
        recordItemEvent(context, item, 'smart-offer-form-detected', { formDetected: Boolean(form), createButtonDetected: Boolean(submit) }, targetWindow);
        if (!repeatCount) return { valid: false, message: 'Campo Quantas vezes oferecer não encontrado.' };
        setValue(offerAmount, item.amountPerOffer, targetWindow); setValue(requestAmount, item.requestAmountPerOffer, targetWindow);
        const configuredMaxTravelHours = Math.max(1, amount(item.maxTravelHours || context.smartOfferConfig?.maxTravelHours || DEFAULT_MAX_TRAVEL_HOURS)); let maxTimeInput = doc.querySelector(MARKET_SELECTORS.maxTravelTime);
        recordItemEvent(context, item, 'smart-offer-max-time-search', { multiFound: Boolean(repeatCount), maxTimeFound: Boolean(maxTimeInput), configuredMaxTravelHours, displayedMaxTravelHours: maxTimeInput?.value ?? null, pageMode: new URL(targetWindow.location.href).searchParams.get('mode'), currentUrl: targetWindow.location.href }, targetWindow);
        if (!maxTimeInput) maxTimeInput = await waitForMaxTravelTimeInput(doc, 5000, targetWindow);
        if (maxTimeInput) recordItemEvent(context, item, 'smart-offer-max-time-found', { maxTimeFound: true, configuredMaxTravelHours, displayedMaxTravelHours: maxTimeInput.value }, targetWindow);
        const durationResult = setMaxTravelHours(doc, configuredMaxTravelHours, targetWindow, maxTimeInput); item.durationDiagnostics = durationResult;
        if (!durationResult.valid) { recordItemEvent(context, item, maxTimeInput ? 'smart-offer-max-time-validation-failed' : 'smart-offer-max-time-not-found', { multiFound: Boolean(repeatCount), maxTimeFound: Boolean(maxTimeInput), configuredMaxTravelHours, displayedMaxTravelHours: maxTimeInput?.value ?? null, durationResult }, targetWindow); return { valid: false, message: durationResult.message }; }
        recordItemEvent(context, item, 'smart-offer-max-time-filled', { multiFound: true, maxTimeFound: Boolean(maxTimeInput), configuredMaxTravelHours, displayedMaxTravelHours: durationResult.value, selector: durationResult.selector }, targetWindow);
        const repeatResult = await setOfferRepeatCount(doc, item, targetWindow); if (!repeatResult.valid) return repeatResult;
        if (Number(repeatCount.value) !== Number(item.repeatCount)) return { valid: false, message: `Não foi possível configurar a quantidade de repetições. Esperado: ${item.repeatCount}. Atual: ${repeatCount.value}.` };
        if (submit) submit.disabled = false; item.status = 'prepared'; item.error = null; item.repeatFillDiagnostics = repeatResult.diagnostics; item.previousSnapshot = snapshotOffers(doc, item); item.beforeOfferCount = item.previousSnapshot.offerCount; item.beforeMatchingOfferCount = item.previousSnapshot.matchingOffers; item.beforeMatchingQuantity = item.previousSnapshot.matchingQuantity; recordItemEvent(context, item, 'smart-offer-form-values-applied', { formDetected: true, createButtonDetected: Boolean(submit), beforeOfferCount: item.beforeOfferCount, beforeMatchingOfferCount: item.beforeMatchingOfferCount, maxTravelHours: item.maxTravelHours, durationResult }, targetWindow); save(context);
        return { valid: true, item, form, submit, message: `Oferta preparada: ${item.repeatCount} × ${item.offerAmount} por ${item.requestAmount}. Clique em Criar no jogo.` };
    };
    const finishSuccess = (context, item, result, targetWindow) => {
        logMarketOfferExecution('current-index-before', summarizeOfferExecution(context));
        item.status = 'created'; item.createdAt = Date.now(); item.offerId = result.offerId; item.confirmation = { evidence: result.evidence, detectedAmount: result.detectedAmount, detectedQuantity: result.detectedQuantity };
        EAS.MarketEngine.applyCreatedOfferToCache(item); addHistory(item, 'created'); syncIndex(context); save(context);
        logMarketOfferExecution('item-marked-created', summarizeOfferExecution(context)); logMarketOfferExecution('current-index-after', summarizeOfferExecution(context));
        emit({ type: 'offer-created', executionId: context.executionId, queueItemId: item.id, villageId: item.villageId, offerResource: item.offerResource, offerAmount: item.offerAmount, requestResource: item.requestResource, requestAmount: item.requestAmount, repeatCount: item.repeatCount, createdAt: item.createdAt });
        try { EAS.MarketEngine.refreshCurrentMarketVillageFromPage(targetWindow.document, item.villageId); emit({ type: 'market-village-refreshed', executionId: context.executionId, villageId: item.villageId }); } catch { EAS.MarketEngine.refreshMarketVillage(item.villageId).then(() => emit({ type: 'market-village-refreshed', executionId: context.executionId, villageId: item.villageId })).catch(() => {}); }
        emit({ type: 'offer-queue-updated', executionId: context.executionId, currentIndex: context.currentIndex });
        const nextItem = current(context); logMarketOfferExecution(nextItem ? 'next-item-found' : 'next-item-missing', { summary: summarizeOfferExecution(context), nextItem }); if (nextItem) recordItemEvent(context, nextItem, 'smart-offer-next-item-start', {}, targetWindow); if (!nextItem) { logMarketOfferExecution('smart-offer-execution-completed', { executionId: context.executionId, summary: summarizeOfferExecution(context) }); logMarketOfferExecution('execution-finished', summarizeOfferExecution(context)); emit({ type: 'offer-execution-finished', executionId: context.executionId }); }
        return nextItem;
    };
    const getNextPendingOfferItem = (execution) => getCurrentPendingOfferItem(execution);
    const advanceMarketOfferExecution = async ({ context, item, result, targetWindow, render = () => {}, prepareNext = async () => {} }) => {
        const nextItem = finishSuccess(context, item, result, targetWindow); render('Oferta criada com sucesso.', 'success');
        await new Promise((resolve) => targetWindow.setTimeout(resolve, 700));
        if (!nextItem) {
            const villageIds = [...new Set(context.queue.filter((entry) => entry.status === 'created').map((entry) => entry.villageId))];
            Promise.allSettled(villageIds.map((villageId) => EAS.MarketEngine.refreshMarketVillage(villageId))).then(() => emit({ type: 'market-villages-refreshed', executionId: context.executionId, villageIds }));
            render('', 'success'); return { state: 'finished', nextItem: null };
        }
        if (getVillageId(targetWindow) === String(nextItem.villageId)) { logMarketOfferExecution('same-village-next', { summary: summarizeOfferExecution(context), nextItem }); render('Preparando próxima oferta...', 'info'); await prepareNext(); return { state: 'same-village', nextItem }; }
        const targetUrl = String(marketUrl(nextItem.villageId)); logMarketOfferExecution('different-village-next', { summary: summarizeOfferExecution(context), nextItem }); logMarketOfferExecution('navigation-start', { currentVillageId: getVillageId(targetWindow), nextVillageId: nextItem.villageId, nextItemId: nextItem.id, targetUrl }); logMarketOfferExecution('navigation-target', { targetUrl }); render(`Abrindo próxima aldeia: ${nextItem.villageName}.`, 'info'); save(context); cleanupMarketOfferRuntime(targetWindow); targetWindow.location.assign(targetUrl); return { state: 'different-village', nextItem };
    };
    const finishError = (context, item, message, render) => { logMarketOfferExecution('offer-error-detected', { message, itemId: item.id, summary: summarizeOfferExecution(context) }); item.status = 'error'; item.error = message; addHistory(item, 'error', message); save(context); emit({ type: 'offer-error', executionId: context.executionId, queueItemId: item.id, error: message }); render(message, 'error'); };
    const mount = (targetWindow = window) => {
        logMarketOfferExecution('auto-init-start', { url: targetWindow.location.href }); const context = read(); if (!context) { logMarketOfferExecution('execution-missing'); logMarketOfferExecution('auto-init-skipped', { reason: 'no-active-execution' }); return false; } logMarketOfferExecution('execution-found', summarizeOfferExecution(context)); syncIndex(context); const item = current(context); logMarketOfferExecution(item ? 'current-item-found' : 'current-item-missing', summarizeOfferExecution(context)); const url = new URL(targetWindow.location.href); const screen = url.searchParams.get('screen'); const mode = url.searchParams.get('mode');
        if (screen !== 'market' || mode !== 'own_offer') { logMarketOfferExecution('auto-init-skipped', { reason: 'wrong-screen', screen, mode }); return false; }
        const doc = targetWindow.document; const runtime = getRuntime(targetWindow); const existingPanel = doc.getElementById(PANEL_ID); if (runtime.initialized && existingPanel) { logMarketOfferExecution('runtime-already-initialized', { summary: summarizeOfferExecution(context) }); logMarketOfferExecution('panel-reuse', { panelId: PANEL_ID }); return true; } runtime.initialized = true; const correctVillage = !item || getVillageId(targetWindow) === String(item.villageId); if (!correctVillage) logMarketOfferExecution('auto-init-skipped', { reason: 'wrong-village', currentVillageId: getVillageId(targetWindow), expectedVillageId: item?.villageId }); let panel = existingPanel; if (!panel) { panel = doc.createElement('aside'); panel.id = PANEL_ID; panel.className = 'fake-execution-panel market-offers-execution-panel'; doc.body.appendChild(panel); logMarketOfferExecution('panel-create', { panelId: PANEL_ID }); } else logMarketOfferExecution('panel-reuse', { panelId: PANEL_ID }); let observer = null; let timeout = null; let clickListener = null; let preparedSubmit = null; let submissionLocked = false;
        const stop = () => { observer?.disconnect(); observer = null; runtime.observer = null; clearTimeout(timeout); runtime.timers.delete(timeout); timeout = null; if (clickListener) { doc.removeEventListener('click', clickListener, true); clickListener = null; } };
        const render = (message = '', type = 'info') => {
            syncIndex(context); const active = current(context); const created = context.queue.filter((entry) => entry.status === 'created').length; const skipped = context.queue.filter((entry) => entry.status === 'skipped').length; const errors = context.queue.filter((entry) => ['error', 'verification-required'].includes(entry.status)).length; const sameVillage = active && getVillageId(targetWindow) === String(active.villageId); const finished = !active;
            panel.innerHTML = `<div class="fake-execution-header"><strong>Criação de Ofertas</strong></div><div class="fake-execution-content"><div class="fake-execution-summary"><div><strong>Aldeia</strong><span>${EAS.Utils.escapeHtml(active?.villageName || '-')}</span></div><div><strong>Oferta</strong><span>${active ? `${active.repeatCount} × ${active.offerAmount} ${active.offerResource} por ${active.requestAmount} ${active.requestResource}` : '-'}</span></div><div><strong>Total</strong><span>${active ? `${active.totalOfferAmount} por ${active.totalRequestAmount}` : '-'}</span></div><div><strong>Comerciantes</strong><span>${active?.merchantsRequired || 0}</span></div><div><strong>Progresso</strong><span>${created} de ${context.queue.length} concluídas</span></div><div><strong>Estado</strong><span>${finished ? 'Execução concluída' : !sameVillage ? 'Aldeia incorreta' : active.status}</span></div></div>${finished ? `<div class="eas-status eas-status--success">Execução concluída. Criadas: ${created}. Puladas: ${skipped}. Erros: ${errors}.</div>` : `<div class="eas-status eas-status--${type}">${EAS.Utils.escapeHtml(message || (!sameVillage ? 'A oferta atual pertence a outra aldeia.' : 'Carregando execução...'))}</div>`}<div class="fake-execution-actions" data-actions></div></div>`;
            const actions = panel.querySelector('[data-actions]'); const add = (text, handler, disabled = false) => { const button = doc.createElement('button'); button.className = 'eas-button'; button.textContent = text; button.disabled = disabled; button.onclick = handler; actions.appendChild(button); };
            add(active?.status === 'submitting' ? 'Criando...' : 'Criar esta oferta', () => submitCurrent(), submissionLocked || !active || !sameVillage || active.status !== 'prepared' || !preparedSubmit);
            add('Preparar novamente', () => arm(true), runtime.preparing || !active || !sameVillage || active.status === 'submitting');
            add(sameVillage ? 'Abrir próxima aldeia' : 'Abrir aldeia correta', () => openCurrent(context), !active || sameVillage);
            add('Repetir oferta com erro', () => { active.status = 'pending'; active.error = null; save(context); arm(); }, !active || !sameVillage || !['error', 'verification-required'].includes(active.status));
            add('Confirmar manualmente como concluída', () => { if (!active || active.status !== 'verification-required') return; const result = { success: true, evidence: 'manual-confirmation', detectedAmount: active.offerAmount, detectedQuantity: active.repeatCount, offerId: null }; recordItemEvent(context, active, 'smart-offer-created-offer-found', { evidence: result.evidence }, targetWindow); advanceMarketOfferExecution({ context, item: active, result, targetWindow, render, prepareNext: arm }); }, !active || active.status !== 'verification-required');
            add('Pular oferta', () => { active.status = 'skipped'; addHistory(active, 'skipped'); syncIndex(context); save(context); emit({ type: 'offer-queue-updated', executionId: context.executionId }); const next = current(context); if (next && getVillageId(targetWindow) === String(next.villageId)) arm(); else render(next ? `Próxima oferta: ${next.villageName}.` : '', 'info'); }, !active || active.status === 'submitting');
            add('Atualizar dados desta aldeia', () => { try { EAS.MarketEngine.refreshCurrentMarketVillageFromPage(doc, getVillageId(targetWindow)); render('Dados da aldeia atualizados.', 'success'); emit({ type: 'market-village-refreshed', executionId: context.executionId, villageId: getVillageId(targetWindow) }); } catch (error) { render(error.message, 'error'); } }, !active);
            add('Recalcular ofertas pendentes', () => recalculatePending(), !active);
            add('Copiar diagnóstico', async () => { const text = JSON.stringify(exportDiagnostic(targetWindow), null, 2); try { await targetWindow.navigator.clipboard.writeText(text); render('Diagnóstico copiado.', 'success'); } catch (error) { logMarketOfferExecution('unexpected-error', { stage: 'copy-diagnostic', message: error.message }); targetWindow.prompt('Copie o diagnóstico:', text); } });
            add('Limpar diagnóstico', () => { clearDebugLogs(); render('Diagnóstico limpo. A fila foi preservada.', 'info'); });
            if (finished) add('Atualizar todos os dados', () => EAS.MarketEngine.refreshAllVillages().then(() => render('', 'success')));
            add('Voltar ao menu', () => returnToMarketMenu({ targetWindow, context, render }));
            add('Encerrar execução', () => { if (targetWindow.confirm('Encerrar a execução? As ofertas restantes serão canceladas.')) { stop(); cleanupMarketOfferRuntime(targetWindow); context.endedAt = Date.now(); context.queue.forEach((entry) => { if (!TERMINAL.has(entry.status)) entry.status = 'cancelled'; }); syncIndex(context); save(context); render('Execução encerrada. Use “Voltar ao menu” para retornar e fechar esta aba, ou permaneça aqui.', 'info'); } });
        };
        const verify = () => {
            const active = current(context); if (!active || active.status !== 'submitting') return;
            const error = errorMessage(targetWindow.document); if (error) { stop(); submissionLocked = false; finishError(context, active, error, render); return; }
            const result = detectOfferCreationSuccess({ document: targetWindow.document, expectedOffer: active, previousSnapshot: active.previousSnapshot });
            if (result.success) { logMarketOfferExecution('offer-success-detected', { itemId: active.id, result, summary: summarizeOfferExecution(context) }); stop(); submissionLocked = false; advanceMarketOfferExecution({ context, item: active, result, targetWindow, render, prepareNext: arm }); }
        };
        const beginVerification = () => {
            const active = current(context); if (!active || active.status === 'submitting') return;
            logMarketOfferExecution('offer-submit-start', { itemId: active.id, summary: summarizeOfferExecution(context) }); active.previousSnapshot = active.previousSnapshot || snapshotOffers(doc, active); active.status = 'submitting'; active.submittedAt = Date.now(); active.clickTimestamp ||= active.submittedAt; recordItemEvent(context, active, 'smart-offer-create-click-start', { beforeOfferCount: active.previousSnapshot.offerCount, beforeMatchingOfferCount: active.previousSnapshot.matchingOffers, clickTimestamp: active.clickTimestamp }, targetWindow); save(context); render('Aguardando confirmação real do jogo...', 'info');
            observer = new targetWindow.MutationObserver(verify); runtime.observer = observer; observer.observe(doc.querySelector('#own_offers_table, #content_value, main') || doc.body, { childList: true, subtree: true, characterData: true });
            timeout = setTimeout(() => { stop(); submissionLocked = false; const pending = current(context); if (pending?.status !== 'submitting') return; const result = detectCreatedSmartOffer(pending, doc, pending.previousSnapshot); pending.tableSnapshot = result.snapshot; if (result.success) { recordItemEvent(context, pending, 'smart-offer-created-offer-found', { evidence: result.evidence, phase: 'verification-timeout' }, targetWindow); advanceMarketOfferExecution({ context, item: pending, result, targetWindow, render, prepareNext: arm }); return; } pending.status = 'verification-required'; pending.error = 'Não foi possível confirmar a criação na tabela Suas ofertas.'; recordItemEvent(context, pending, 'smart-offer-verification-required', { phase: 'verification-timeout', tableSnapshot: result.snapshot }, targetWindow); save(context); emit({ type: 'offer-verification-required', executionId: context.executionId, queueItemId: pending.id }); render(pending.error, 'error'); }, 10000);
            runtime.timers.add(timeout);
        };
        const arm = async (force = false) => {
            const queueItem = current(context); if (!queueItem) return; const debug = localStorage.getItem('eas_tw_market_offers_debug') === 'true';
            if (force) { runtime.preparedItemId = null; runtime.preparing = false; runtime.paused = false; runtime.prepareAttempts.set(queueItem.id, []); }
            if (runtime.paused || runtime.preparing) return;
            if (!force && runtime.preparedItemId === queueItem.id && queueItem.status === 'prepared') { if (debug) console.debug('[EAS Offer Executor]', { event: 'prepare-skipped-already-prepared', itemId: queueItem.id }); return; }
            const attempt = registerPrepareAttempt(runtime, queueItem.id); if (!attempt.allowed) { cleanupMarketOfferRuntime(targetWindow); runtime.paused = true; render('Execução pausada por repetição inesperada. Nenhuma nova ação será realizada até você clicar em Preparar novamente.', 'error'); return; }
            runtime.preparing = true; queueItem.status = 'preparing'; save(context); logMarketOfferExecution('offer-prepare-start', { itemId: queueItem.id, summary: summarizeOfferExecution(context) }); if (debug) console.debug('[EAS Offer Executor]', { event: 'prepare-start', itemId: queueItem.id, status: queueItem.status });
            try {
                stop(); render('Aguardando o formulário do jogo estabilizar...', 'info'); const prepared = await prepareItem(context, targetWindow); const latest = current(context); if (!latest || latest.id !== queueItem.id) return;
                if (!prepared.valid) { runtime.preparing = false; render(prepared.message, 'error'); return; }
                runtime.preparedItemId = queueItem.id; preparedSubmit = prepared.submit; runtime.preparing = false; logMarketOfferExecution('offer-prepared', { itemId: queueItem.id, summary: summarizeOfferExecution(context) }); render('Oferta preparada.', 'success'); if (debug) console.debug('[EAS Offer Executor]', { event: 'prepare-finish', itemId: queueItem.id });
                clickListener = (event) => { const submit = event.target.closest?.('[name="create_offer"], button[type="submit"], input[type="submit"], [data-action="create-offer"]'); if (!submit || (prepared.form && submit.form && submit.form !== prepared.form)) return; doc.removeEventListener('click', clickListener, true); clickListener = null; beginVerification(); };
                doc.addEventListener('click', clickListener, true);
            } catch (error) { runtime.paused = true; logMarketOfferExecution('unexpected-error', { stage: 'prepare', message: error.message, stack: error.stack, itemId: queueItem.id, summary: summarizeOfferExecution(context) }); render('Execução pausada. Copie o diagnóstico.', 'error'); } finally { runtime.preparing = false; }
        };
        const submitCurrent = () => {
            const active = current(context); if (submissionLocked || !active || active.status !== 'prepared' || !preparedSubmit) return;
            const repeatInput = findRepeatField(doc); const offerInput = findField(doc, ['sell', 'offer_amount', 'amount_sell']); const requestInput = findField(doc, ['buy', 'request_amount', 'amount_buy']);
            if (Number(repeatInput?.value) !== active.repeatCount || Number(offerInput?.value) !== active.amountPerOffer || Number(requestInput?.value) !== active.requestAmountPerOffer) { render('Os campos foram alterados. Prepare novamente antes de criar.', 'error'); return; }
            submissionLocked = true; active.previousSnapshot = snapshotOffers(doc, active); active.beforeOfferCount = active.previousSnapshot.offerCount; active.beforeMatchingOfferCount = active.previousSnapshot.matchingOffers; active.beforeMatchingQuantity = active.previousSnapshot.matchingQuantity; save(context); beginVerification(); recordItemEvent(context, active, 'smart-offer-create-button-found', { createButtonDetected: true }, targetWindow); preparedSubmit.click(); recordItemEvent(context, active, 'smart-offer-create-clicked', { clickTimestamp: active.clickTimestamp }, targetWindow);
        };
        const recalculatePending = () => {
            let config = {}; try { config = JSON.parse(localStorage.getItem('eas_tw_market_offers_config') || '{}'); } catch {}
            const result = EAS.MarketEngine.buildGlobalOfferSuggestions(EAS.MarketEngine.cacheVillages(EAS.MarketEngine.getCache()), config); const preserved = context.queue.filter((entry) => TERMINAL.has(entry.status)); context.queue = [...preserved, ...result.suggestions.map(normalizeItem)]; context.currentIndex = preserved.length; syncIndex(context); save(context); emit({ type: 'offer-queue-updated', executionId: context.executionId }); render('Ofertas pendentes recalculadas.', 'success');
        };
        render();
        if (!item) render('', 'success');
        else if (!correctVillage) render('A oferta atual pertence a outra aldeia.', 'info');
        else if (item.status === 'submitting') { recordItemEvent(context, item, 'smart-offer-navigation-detected', { navigationTimestamp: Date.now(), currentUrl: targetWindow.location.href }, targetWindow); recordItemEvent(context, item, 'smart-offer-page-reloaded', { reloadDetected: true, currentUrl: targetWindow.location.href }, targetWindow); const reconcileTimer = targetWindow.setTimeout(() => { runtime.timers.delete(reconcileTimer); const afterSnapshot = snapshotOffers(doc, item); item.afterOfferCount = afterSnapshot.offerCount; item.afterMatchingOfferCount = afterSnapshot.matchingOffers; item.afterMatchingQuantity = afterSnapshot.matchingQuantity; item.tableSnapshot = afterSnapshot; recordItemEvent(context, item, 'smart-offer-own-offers-detected', { ownOffersDetected: afterSnapshot.ownOffersDetected, afterOfferCount: item.afterOfferCount, afterMatchingOfferCount: item.afterMatchingOfferCount, afterMatchingQuantity: item.afterMatchingQuantity }, targetWindow); const result = detectCreatedSmartOffer(item, doc, item.previousSnapshot); if (result.success) { recordItemEvent(context, item, 'smart-offer-created-offer-found', { evidence: result.evidence, beforeMatchingOfferCount: item.beforeMatchingOfferCount, afterMatchingOfferCount: item.afterMatchingOfferCount }, targetWindow); advanceMarketOfferExecution({ context, item, result, targetWindow, render, prepareNext: arm }); } else { recordItemEvent(context, item, 'smart-offer-created-offer-not-found', { beforeMatchingOfferCount: item.beforeMatchingOfferCount, afterMatchingOfferCount: item.afterMatchingOfferCount }, targetWindow); item.status = 'verification-required'; item.error = 'A oferta não foi confirmada na tabela Suas ofertas. Confirme antes de repetir.'; item.verificationRequiredAt = Date.now(); recordItemEvent(context, item, 'smart-offer-verification-required', { persistedStatus: item.status, tableSnapshot: afterSnapshot }, targetWindow); save(context); emit({ type: 'offer-verification-required', executionId: context.executionId, queueItemId: item.id }); render(item.error, 'error'); } }, 500); runtime.timers.add(reconcileTimer); }
        else if (['pending', 'opening', 'prepared'].includes(item.status)) arm();
        return true;
    };
    EAS.MarketOffersExecution.start = (context) => { const maxTravelHours = Math.max(1, amount(context.smartOfferConfig?.maxTravelHours || context.maxTravelHours || DEFAULT_MAX_TRAVEL_HOURS)); const queue = (context.queue || []).map((item) => normalizeItem({ ...item, maxTravelHours: item.maxTravelHours || maxTravelHours, status: 'pending', error: null })); const normalized = { ...context, smartOfferConfig: { ...(context.smartOfferConfig || {}), maxTravelHours }, calculationMode: context.calculationMode || 'per-village', version: EXECUTION_VERSION, executionId: context.executionId || `market-${Date.now()}-${Math.random().toString(36).slice(2)}`, openerExpected: true, returnTarget: 'market-offers-menu', world: EAS.World.getWorldName(), createdAt: Date.now(), currentIndex: 0, queue }; logMarketOfferExecution('smart-offer-execution-start', { executionId: normalized.executionId, queueLength: queue.length, maxTravelHours }); queue.forEach((item) => recordItemEvent(normalized, item, 'smart-offer-item-loaded', { maxTravelHours }, window)); syncIndex(normalized); save(normalized); emit({ type: 'execution-started', executionId: normalized.executionId }); openCurrent(normalized); return true; };
    const initializeMarketOfferExecutionIfNeeded = (targetWindow = window) => { const url = new URL(targetWindow.location.href); const pageData = { screen: url.searchParams.get('screen'), mode: url.searchParams.get('mode'), villageId: getVillageId(targetWindow), url: url.href }; logMarketOfferExecution('page-loaded', pageData); logMarketOfferExecution('auto-init-start'); if (isOwnOfferPage(targetWindow)) logMarketOfferExecution('smart-offer-page-detected', pageData); else { logMarketOfferExecution('auto-init-skipped', { reason: 'wrong-screen', ...pageData }); return false; } const execution = read(); if (!execution) { logMarketOfferExecution('execution-missing'); logMarketOfferExecution('smart-offer-resume-skipped-no-active-execution', pageData); return false; } logMarketOfferExecution('execution-found', summarizeOfferExecution(execution)); const item = getCurrentPendingOfferItem(execution); logMarketOfferExecution(item ? 'current-item-found' : 'current-item-missing', summarizeOfferExecution(execution)); if (!item && !execution.queue?.length) { logMarketOfferExecution('auto-init-skipped', { reason: 'no-current-item' }); return false; } if (item && getVillageId(targetWindow) !== String(item.villageId)) { logMarketOfferExecution('auto-init-skipped', { reason: 'wrong-village', currentVillageId: getVillageId(targetWindow), expectedVillageId: item.villageId }); return false; } return mount(targetWindow); };
    EAS.MarketOffersExecution.initialize = () => initializeMarketOfferExecutionIfNeeded(window);
    window.EASMarketOffersDebug = { getLogs: getDebugLogs, clearLogs: clearDebugLogs, getExecution: read, getRuntime: () => getRuntime(window), exportDiagnostic: () => exportDiagnostic(window) };
    if (localStorage.getItem('eas_tw_market_offers_debug') === 'true' && !window.__easMarketOffersErrorLogging) { window.__easMarketOffersErrorLogging = true; window.addEventListener('error', (event) => logMarketOfferExecution('unexpected-error', { message: event.message, stack: event.error?.stack, file: event.filename, line: event.lineno, item: current(read()), url: location.href })); window.addEventListener('unhandledrejection', (event) => logMarketOfferExecution('unexpected-error', { message: event.reason?.message || String(event.reason), stack: event.reason?.stack, item: current(read()), url: location.href })); }
    registerMarketOfferMenuListener(window);
    Object.assign(EAS.MarketOffersExecution, { STORAGE_KEY, DEBUG_LOG_KEY, CHANNEL_NAME, EXECUTION_WINDOW_KEY, RETURN_MESSAGE_SOURCE, EXECUTION_VERSION, DEFAULT_MAX_TRAVEL_HOURS, MARKET_SELECTORS, PANEL_ID, isOwnOfferPage, read, save, remove, current, getCurrentPendingOfferItem, getNextPendingOfferItem, syncIndex, nextPendingIndex, normalizeItem, getRuntime, getMenuRuntime, cleanupMarketOfferRuntime, registerPrepareAttempt, getDebugLogs, clearDebugLogs, logMarketOfferExecution, recordItemEvent, summarizeOfferExecution, completionSummary, isExecutionFinished, refreshExistingHubPanel, handleReturnMessage, registerMarketOfferMenuListener, renderReturnFallback, returnToMarketMenu, exportDiagnostic, injectLoader, watchExecutionWindow, openCurrent, mount, initializeMarketOfferExecutionIfNeeded, validateQueueItem, prepareItem, findRepeatField, fillRepeatCount, setOfferRepeatCount, waitForElement, waitForMaxTravelTimeInput, setMaxTravelHours, snapshotOffers, detectOfferCreationSuccess, detectCreatedSmartOffer, classifyOfferResult, errorMessage, finishSuccess, advanceMarketOfferExecution, finishError });
})();
